{
  "name": "spectatr-linkedin-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "agent": "ts-node -r dotenv/config agent/orchestrator.ts",
    "dev": "NODE_ENV=development ts-node -r dotenv/config agent/orchestrator.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "netlify-cli": "^24.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
